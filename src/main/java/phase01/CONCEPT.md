# Phase 1: Java 基础语法 - 核心概念

> 编程的本质是用代码描述数据和处理数据的过程

---

## 🎯 Java 语言全景图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Java 基础语法                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  程序结构                                                           │
│  ├── 类 (Class) - 程序的基本组织单元                               │
│  ├── 方法 (Method) - 执行特定任务的代码块                          │
│  └── 语句 (Statement) - 执行的基本单位                             │
│                                                                     │
│  数据处理                                                           │
│  ├── 变量 - 存储数据的容器                                         │
│  ├── 数据类型 - 定义数据的种类                                     │
│  └── 运算符 - 操作数据的符号                                       │
│                                                                     │
│  流程控制                                                           │
│  ├── 条件语句 - if, switch                                        │
│  ├── 循环语句 - for, while, do-while                              │
│  └── 跳转语句 - break, continue, return                           │
│                                                                     │
│  数据结构                                                           │
│  ├── 数组 - 存储固定大小的同类型元素                               │
│  └── 多维数组 - 数组的数组                                         │
│                                                                     │
│  代码组织                                                           │
│  ├── 方法 - 代码复用的基本单位                                     │
│  └── 递归 - 方法调用自身                                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 数据类型体系

### 基本数据类型 (Primitive Types)

```
┌─────────────────────────────────────────────────────────────────────┐
│                      8 种基本数据类型                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  整数类型                          浮点类型                         │
│  ┌─────────┬─────────┐            ┌─────────┬─────────┐            │
│  │  byte   │ 1 字节  │            │  float  │ 4 字节  │            │
│  │  short  │ 2 字节  │            │  double │ 8 字节  │            │
│  │  int    │ 4 字节  │            └─────────┴─────────┘            │
│  │  long   │ 8 字节  │                                              │
│  └─────────┴─────────┘            字符类型                          │
│                                    ┌─────────┬─────────┐            │
│  布尔类型                          │  char   │ 2 字节  │            │
│  ┌─────────┬─────────┐            └─────────┴─────────┘            │
│  │ boolean │ 1 bit   │                                              │
│  └─────────┴─────────┘                                              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 类型转换

```
自动类型转换（小 → 大）：
byte → short → int → long → float → double
            ↑
          char

强制类型转换（大 → 小）：
int num = (int) 3.14;  // 会丢失精度
```

---

## 🔀 流程控制

### 条件语句

```java
// if-else 结构
if (条件1) {
    // 条件1 为真时执行
} else if (条件2) {
    // 条件2 为真时执行
} else {
    // 其他情况
}

// switch 表达式 (Java 17+)
String result = switch (day) {
    case MONDAY, FRIDAY -> "工作日";
    case SATURDAY, SUNDAY -> "周末";
    default -> "未知";
};
```

### 循环语句

```
┌─────────────────────────────────────────────────────────────────────┐
│                        循环类型对比                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  for 循环             while 循环           do-while 循环            │
│  ─────────────────   ─────────────────   ─────────────────         │
│  • 已知循环次数       • 条件驱动           • 至少执行一次            │
│  • 遍历数组/集合      • 未知循环次数       • 先执行后判断            │
│                                                                     │
│  for (int i=0;       while (条件) {       do {                     │
│       i<n; i++) {        // 循环体            // 循环体             │
│      // 循环体        }                   } while (条件);           │
│  }                                                                  │
│                                                                     │
│  增强 for 循环 (for-each)                                          │
│  for (元素类型 变量 : 数组/集合) {                                  │
│      // 处理每个元素                                                │
│  }                                                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📦 数组

### 数组基础

```java
// 声明和初始化
int[] arr1 = new int[5];           // 动态初始化
int[] arr2 = {1, 2, 3, 4, 5};      // 静态初始化

// 数组特点
// 1. 固定大小 - 创建后不可改变
// 2. 类型一致 - 存储相同类型元素
// 3. 索引访问 - 从 0 开始
// 4. 连续内存 - 随机访问 O(1)
```

### 多维数组

```
┌─────────────────────────────────────────────────────────────────────┐
│                        二维数组                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  int[][] matrix = {                                                 │
│      {1, 2, 3},      ← 第 0 行                                     │
│      {4, 5, 6},      ← 第 1 行                                     │
│      {7, 8, 9}       ← 第 2 行                                     │
│  };                                                                 │
│                                                                     │
│  访问元素: matrix[行][列]                                           │
│  matrix[1][2] = 6                                                   │
│                                                                     │
│  遍历:                                                              │
│  for (int i = 0; i < matrix.length; i++) {                         │
│      for (int j = 0; j < matrix[i].length; j++) {                  │
│          System.out.print(matrix[i][j] + " ");                     │
│      }                                                              │
│  }                                                                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 方法

### 方法结构

```java
修饰符 返回类型 方法名(参数列表) {
    // 方法体
    return 返回值;  // 如果返回类型不是 void
}

// 示例
public static int add(int a, int b) {
    return a + b;
}
```

### 方法重载 (Overload)

```java
// 同名方法，参数不同
public int add(int a, int b) { return a + b; }
public double add(double a, double b) { return a + b; }
public int add(int a, int b, int c) { return a + b + c; }
```

### 递归

```java
// 递归三要素：
// 1. 递归终止条件
// 2. 递归调用
// 3. 问题规模缩小

public int factorial(int n) {
    if (n <= 1) return 1;           // 终止条件
    return n * factorial(n - 1);    // 递归调用
}
```

---

## 📖 学习要点

```
✅ 掌握 8 种基本数据类型及其范围
✅ 理解变量的声明、初始化和作用域
✅ 熟练使用 if-else 和 switch
✅ 掌握三种循环及其适用场景
✅ 理解数组的内存模型
✅ 能够编写和调用方法
✅ 理解递归的工作原理
```

---

> 接下来学习面向对象编程：[Phase 2 README](../phase02/README.md)
